# DiyetX - Streamlit Uygulaması

## Kurulum

```bash
pip install streamlit pandas pillow requests
```

## Çalıştırma

```bash
streamlit run app.py
```

## Netlify Deploy

Build command: `pip install -r requirements.txt`
Publish directory: `.`